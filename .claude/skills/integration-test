#!/bin/bash
# Integration Test Skill Wrapper
# Run the EBF Framework Integration Test Suite
# Usage: integration-test [OPTIONS]

cd "$(git rev-parse --show-toplevel)" || {
    echo "❌ Not in a git repository"
    exit 2
}

python3 scripts/integration_test.py "$@"
exit $?
